apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opencloud.fullname" . }}-test-connection"
  labels:
    {{- include "opencloud.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opencloud.fullname" . }}:9200']
  restartPolicy: Never
